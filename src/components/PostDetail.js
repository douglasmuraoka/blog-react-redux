/**
 * Component responsible for showing the details of a single post,
 * as well as its comments.
 */
import React from 'react';

export default ({ match }) => <div>Details of post: {match.params.id}</div>